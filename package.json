{
  "name": "sas-site",
  "version": "1.0.0",
  "private": true,
  "description": "Secure Asset Intelligence Services — static site generator",
  "scripts": {
    "build": "node build.js",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  }
}
